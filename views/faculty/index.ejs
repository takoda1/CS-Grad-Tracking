<!-- views/faculty/index.ejs -->
<!DOCTYPE html>

<html>
	<head>
		<title>Search Faculty</title>
		<%- include('../partials/head.ejs') %>
	</head>
	
	<body>
		<div class="container-fluid h-100">
			<div class="row h-100">

				<div class='col-lg-2 sidebar text-center'>

					<%- include('../partials/admin-sidebar.ejs') %>

					<%- include('./search-faculty.ejs') %>

				</div>
				
				<div class='col-lg-10 panelbg text-center'>
					<h1>Faculty</h1>
					 <% if(faculty.length>0) { %>
						<table align="center" border="1" class="table display-table table-dark">
							<thead>
								<tr>
									<th scope="col" class="col-facultyUsername">Username</th>
									<th scope="col" class="col-facultyFirstname">First name</th>
									<th scope="col" class="col-facultyLastname">Last Name</th>
									<th scope="col" class="col-facultyPid">PID</th>
									<th scope="col" class="col-facultyActive">Active</th>
									<th scope="col" class="col-facultyDelete"></th>
								</tr>
							</thead>
							<tbody>

								<% for(var i=0; i<faculty.length; i++) { %>
									<tr>
										<td><%= faculty[i].username%></td>
										<td><%= faculty[i].firstName%></td>
										<td><%= faculty[i].lastName%></td>
										<td><%= faculty[i].pid%></td>
										<td><%= faculty[i].active%></td>
										<td>
											<div class="col-md-10 offset-md-1">
												<a href="/faculty/edit/<%=faculty[i]._id%>" class="editTag"><button class="btn btn-light btn-block">Edit</button></a>
												<form action="/faculty/delete/<%=faculty[i]._id%>" method="post">
													<button type="submit" class="btn btn-danger btn-block" onclick="return confirm('Do you really want to delete this?');">Delete</button>
												</form>
											</div>
										</td>
									</tr>
								<% } %>
							</tbody>
						</table>
					<% } else { %>
						<div>No Faculty found.</div>
					<% } %>
				</div>
			</div>
		</div>
	</body>
</html>